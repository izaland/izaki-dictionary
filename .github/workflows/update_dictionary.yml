name: Update Izaki Dictionary

on:
  push:
    paths:
      - 'data/compounds.csv'
  workflow_dispatch:

jobs:
  build-dictionary:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
      
      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: 🔨 Build complete dictionary.json
        run: |
          python scripts/build_dictionary.py
      
      - name: 🔄 Generate IPA from compounds
        run: |
          python scripts/generate_ipa.py
      
      - name: 📤 Commit updated files
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/dictionary.json data/dictionary_with_ipa.json
          git diff --staged --quiet || git commit -m "🤖 Auto-update dictionary files from CSV"
      
      - name: 🚀 Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}
